/**
 * Uploads Module — Barrel Export (Phase 2)
 * 
 * Creative Memory: files as first-class AI inputs.
 */

export {
  uploadCreativeMemory,
  buildCreativeContext,
  injectCreativeMemory,
  extractFileMetadata,
  type CreativeMemoryRole,
  type CreativeMemoryAsset,
  type ExtractedMetadata,
  type UploadResult,
} from './creativeMemory';
